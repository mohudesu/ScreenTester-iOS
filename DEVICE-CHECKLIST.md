# 待在 Mac / iPhone 验证的项目

当前状态：以下检查均未执行，Windows 环境无法替代 Xcode 编译和 iPhone 实机测试。

- [ ] `bash verify-on-mac.command` 模拟器编译通过。
- [ ] 生成未签名 IPA 后，由用户自行签名并安装到 iPhone 17，确认启动成功。
- [ ] 记录 macOS / Xcode / iOS 版本及手机显示缩放设置。
- [ ] 所有首页项目均能打开；非 HDR 测试均可长按 2 秒退出；纯色轻点能循环八色。
- [ ] 黑边图覆盖屏幕可绘制区域，无安全区留白；系统状态栏和底部指示条隐藏行为正常。
- [ ] 未贴膜情况下校准四角，检查圆弧近似与屏幕实际曲线的误差；贴膜后观察遮挡。
- [ ] 精度分区在显示标准模式下确认为 2/4/1/6/8 物理像素；若无法确认，不作为精密计量工具。
- [ ] 线宽、颜色、四角、PPI 设置在退出重启后保留。
- [ ] 灰阶、纯灰和彩条全屏，无永久提示条遮盖。
- [ ] 触控滑动填格，多指按下/抬起正确更新；系统手势取消触摸后无残留触点。
- [ ] 触控频率在持续单指滑动时有值，停止后回到提示；多指不会累加成虚假的更高 Hz。
- [ ] HDR 系统余量信息可读，页面未把系统数据误标为实测亮度。
- [ ] 临时最高亮度退出测试恢复；回到桌面、锁屏或来电打断也恢复，回来后继续应用设置。
- [ ] `bash build-ipa.command` 生成未签名 IPA，包含 Payload/ScreenTester.app 和 arm64 可执行文件。
- [ ] 验证最终 IPA 的描述文件覆盖此设备且未过期，安装并启动；记录到期日期。

若有失败，提供 `output` 中对应构建日志或 Xcode 完整报错，继续修正后重测相关项目。不要上传私钥或 Apple 账号密码。
